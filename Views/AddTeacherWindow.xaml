<Window x:Class="IntellectFlow.Views.AddTeacherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить преподавателя" Height="350" Width="500"
        Background="#FFF9FAFC" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Thickness x:Key="DefaultMargin">0,5,0,10</Thickness>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="{StaticResource DefaultMargin}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True" Margin="20">
        <!-- Кнопки снизу -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical">
            <Separator Margin="0,10,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Добавить" Click="AddButton_Click"/>
                <Button Content="Отмена" Background="#AAA" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Форма -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Имя:"/>
                <TextBox Name="NameTextBox"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Отчество:"/>
                <TextBox Name="MidleNameTextBox"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Фамилия:"/>
                <TextBox Name="LastNameTextBox"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
