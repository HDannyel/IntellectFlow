<Window x:Class="IntellectFlow.Views.StudentSubmissionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:IntellectFlow.Helpers"
        Title="Задания студента" Height="500" Width="900">
    <Window.Resources>
        <local:NullToBooleanConverter x:Key="NullToFalseConverter" />
    </Window.Resources>
    <Grid Margin="10">
        <ListView ItemsSource="{Binding StudentAssignments}" 
                  SelectedItem="{Binding SelectedAssignment}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Задание" Width="250" DisplayMemberBinding="{Binding Assignment.Title}" />
                    <GridViewColumn Header="Статус" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayStatus}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Gray" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Submission.Status}" Value="Checked">
                                                    <Setter Property="Foreground" Value="Green" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Submission.Status}" Value="Rejected">
                                                    <Setter Property="Foreground" Value="Red" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Submission.Status}" Value="Pending">
                                                    <Setter Property="Foreground" Value="Orange" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Оценка -->
                    <GridViewColumn Header="Оценка" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding TeacherScore, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Комментарий -->
                    <GridViewColumn Header="Комментарий">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding TeacherComment, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Кнопка открытия файла -->
                    <GridViewColumn Header="Файл" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Открыть"
                    Command="{Binding DataContext.OpenSubmissionFileCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                    IsEnabled="{Binding SubmissionFilePath, Converter={StaticResource NullToFalseConverter}}"
                    ToolTip="Открыть файл студента" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Кнопка сохранения -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить изменения"
                    Command="{Binding SaveChangesCommand}"
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>